import { StyleSheet, ViewStyle } from "react-native";

export const styles = StyleSheet.create({
    container: {
        height: 60,
        width: '100%',
        borderWidth: 2,
        borderColor: '#242c35',
        borderRadius: 5
    } as ViewStyle,
});